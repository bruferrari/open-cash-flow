# Required metadata
sonar.projectKey=open-cash-flow
sonar.projectName=Open source cash flow project
sonar.projectVersion=0.2b

# Comma-separated paths to directories with sources (required)
sonar.sources=src

# Language
sonar.language=java

# Encoding of the source files
sonar.sourceEncoding=UTF-8